{
  "name": "audio-quest",
  "scripts": {
    "build": "preact build --dest docs --no-sw --no-esm",
    "serve": "sirv build --port 8080 --cors --single",
    "start": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-preact": "^1.1.0",
    "file-loader": "^6.2.0",
    "preact-cli": "^3.3.3",
    "sirv-cli": "1.0.3"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1"
  }
}
